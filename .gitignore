# Build output
/bin/
# GoReleaser's output. `release --clean` wipes it, so nothing tracked may live
# here: the Krew manifest is in krew/ for exactly that reason.
/dist/
# Anchored to the repository root. Without the slash these also match the
# cmd/kubectl-why directory, which silently excluded the program's entrypoint
# from the repository.
/kubectl-why
/kubectl-why.exe

# Test and tooling artifacts
*.test
*.out
coverage.txt

# Editors and OS
.DS_Store
.idea/
.vscode/
